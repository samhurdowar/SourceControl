
<script>

    $(document).ready(function () {

        //hUC9^vA%B.PeUE>(   Failed to load resource: net::ERR_CERT_AUTHORITY_INVALID
    });


    function makeRequest() {
        var requestString = makeRequestJSON();
        var usr = "xz07pm";
        var pwd = "hUC9^vA%B.PeUE>(";
        $.ajax({
            url: "https://cmstswrg003a/ins",
            type: "POST",
            contentType: "application/json",
            callback: "?",
            beforeSend: function (request) {
                request.setRequestHeader("content-type", "application/json");
            },
            headers: {
                "Authorization": "Basic " + usr + ":" + pwd
            },
            dataType: "json",
            data: requestString,
            success: function (data) {
                alert(JSON.stringify(data));
            },
            error: function (request, status, error) {
                alert(request.responseText);
            }
        });
    }

    function makeRequestJSON() {
        var req = {
            'ins_api': {
                'version': '1.0',
                'type': 'cli_show',
                'chunk': '0',
                'sid': '1',
                'input': 'show interface status',
                'output_format': 'json'
            }
        };
        return JSON.stringify(req);
    }


</script>
<h1>NX-API Sandbox</h1>
<br />
<br />


<button onclick="makeRequest()">Test Now</button>

<br />

<br />

